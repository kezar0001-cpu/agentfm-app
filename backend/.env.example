# Database
# For production (e.g., Render), add connection pool params for better performance:
# DATABASE_URL=postgresql://user:password@host:5432/dbname?connection_limit=10&pool_timeout=30&connect_timeout=10
DATABASE_URL=postgresql://user:password@localhost:5432/agentfm

# Redis Cache Configuration
# Use REDIS_URL for standard Redis connection
REDIS_URL=redis://localhost:6379

# Alternative: Use REDIS_TLS_URL for secure Redis connections (e.g., Heroku Redis)
# REDIS_TLS_URL=rediss://user:password@host:port

# Alternative: Configure Redis with separate host/port
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Disable Redis caching entirely (useful for development/testing)
# REDIS_DISABLED=true

# JWT Secret
JWT_SECRET=your-secret-key-here

# Node Environment
NODE_ENV=development

# Server Port
PORT=5000

# Frontend URL (used for invite emails and OAuth redirects)
FRONTEND_URL=http://localhost:5173

# App URL (used for password reset emails, should match FRONTEND_URL in production)
APP_URL=http://localhost:5173

# Cloudinary Configuration (for persistent image storage in production)
# Sign up at https://cloudinary.com to get these values
# Without these, images will be stored locally (NOT recommended for production)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
